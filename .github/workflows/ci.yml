name: My First Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Wait step
        run: sleep 10
      - name: Print message
        run: echo "CI Running Successfully"
